# Harness Patterns

Use dynamic harnesses to control failure modes: early stopping, self-preferential verification, and goal drift. The harness externalizes state, splits independent work, and gives verification a different context from implementation.

Use a quick harness when only one assumption needs adversarial review. Use a full harness when there are many units, long runs, or high scientific risk.
