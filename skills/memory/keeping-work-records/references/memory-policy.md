# Memory Policy

Memory is not a transcript. It is curated operational context.

Active state answers what is happening now. Git notes explain why a commit exists. Lessons store reusable corrections and confirmed approaches. Never let memory override repository facts, test output, profiler output, simulation output, or primary sources.
