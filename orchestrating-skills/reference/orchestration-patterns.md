# Orchestration patterns

## Recon before architecture
Use when architecture depends on existing code.

## Research before implementation
Use when a paper/spec defines the algorithm or constraints.

## Verification before explanation
Use when the final summary must claim correctness, speedup, or scientific validity.

## Domain split
Use separate domain skills when a task crosses boundaries, such as QEC plus GPU performance.
